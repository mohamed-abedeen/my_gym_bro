org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# org.gradle.java.home is machine-specific — set it in ~/.gradle/gradle.properties,
# never here (a committed Windows path breaks CI on Linux runners).
