# STATUS — Verified Code Audit & Task Backlog

> Generated by auditing the actual codebase against the plan on **2026-05-28**. This is the ground-truth "what's done vs. what's left" list. Where the code contradicts an earlier assumption, the code wins (noted inline).

Legend: ✅ done · 🟡 partial · 🔴 not built · ⚠️ remove · `[new]` build from scratch · `[fix]` change existing · `[rm]` delete

---

## 1. Verified Current-State Matrix

| Area | Status | Evidence / Notes |
|------|--------|------------------|
| Local DB (Drift v12) + DAOs | ✅ | `app_database.dart` schema v12; full sync columns |
| Offline sync outbox | ✅ | `sync_service.dart` — real payloads, deferred remote-id |
| Auth (email + Google/Apple) + offline degrade | ✅ | `auth_notifier.dart`, nullable Supabase client |
| RevenueCat purchase/restore + webhook | ✅ | `paywall_screen.dart`, `revenuecat-webhook` |
| Workout core (session, sets, rest timer) | ✅ | `active_session_*`, `rest_timer_service.dart` |
| Schedule builder + exercise browser + seeder | ✅ | `schedule_builder_screen.dart`, `exercise_browser_screen.dart`, `program_seeder.dart` |
| Liquid-glass design system | ✅ | `shared/widgets/glass_*`, `oc_glass_btn.dart` |
| Notification tone system | ✅ | `notification_tone.dart` (4 tones, locale-aware) |
| **Paywall gate** | ✅ | Hardened (2026-05-28): `subscriptionLockedProvider` + GoRouter `redirect` block all non-exempt routes; paywall non-dismissible while locked; status refreshed on start/resume. |
| Anatomy body — recovery | ✅ | `anatomy_body.dart` colors by `recoveryPercent` |
| Anatomy body — volume mode | 🔴 | recovery only; no volume/emphasis mode or toggle |
| **Skins — selection** | 🟡 | `skin_provider.dart` wires **14 skins** (not 3). Picker works. (Earlier audit said 3 — corrected.) |
| Skins — economy (own/earn/buy) | 🔴 | `selectedSkinProvider` is a plain StateProvider; every skin free, no ownership/persistence/IAP/earn rules |
| Leaderboard — scope UI | 🟡 | `leaderboard_screen.dart:23` `enum _Scope {rivals,global,friends}` + tabs (leaderboard/challenges) exist |
| Leaderboard — season boards | 🔴 | no weekly/monthly/all-time board switch; no reset/winners |
| Leaderboard — data/scoring | 🔴 | hardcoded mock rows; no backend, no composite, no normalization |
| Community feed | 🔴 | `MockCommunityRepository` / `community_mock_data.dart`; composer doesn't persist |
| Profile — follower/following UI | 🟡 | `profile_screen.dart:472,478` show hardcoded `'9'`/`'120'` |
| Followers/friends backend | 🔴 | no `follows` table, no mutual-friend logic |
| Challenges backend | 🔴 | mock; no `challenges`/participants/reports tables or moderation |
| Periodic reports (weekly/monthly) | 🔴 | no `user_reports`, no `generate-reports`, no Reports window |
| Training calendar | 🔴 | Home day strip not tappable to a calendar |
| Share cards | 🔴 | not built |
| Progress charts (status log) | 🔴 | not built |
| Wearables / Health | 🔴 | `health` package **not** in `pubspec.yaml`; nothing wired |
| Light-mode orange accent | 🔴 | `constants.dart:63 & 90` — accent is `0xFFD2FF00` (lime) in **both** themes |
| Localization (de/es/fr) | 🟡 | en=377 keys, de/es/fr≈293 (~84 missing each) |
| Notification force-kill resilience | 🟡 | known gap; `docs/notification-recovery.md` |
| DM subsystem | ✅ removed | Deleted app code + Drift table (schema v13) + entry point; Supabase drop in `005_drop_dm.sql` (apply on deploy). |

---

## 2. Task Backlog (by phase)

### Phase 1 — Monetization gate, DM removal, cleanup ✅ DONE (2026-05-28)
- ✅ `[fix]` Hardened the paywall gate: removed the dismissible `context.push` from the scaffold; added a single `subscriptionLockedProvider` (`workout_providers.dart`) consumed by a GoRouter `redirect` (`app_router.dart`) that blocks every non-exempt route. Paywall is non-dismissible while locked (`PopScope` + hidden close button).
- ✅ `[new]` Refresh subscription status via `SubscriptionSyncService.syncNow` on app **start** (`main.dart`) and **resume** (`my_gym_bro_scaffold.dart`); `listen()` installed for live renewals.
- ✅ `[new]` Trial countdown (days left) shown in Settings subscription row (new l10n key `trialDaysLeft`, all 4 locales) via `trialDaysLeftProvider`.
- ✅ `[fix]` Restore Purchases reachable (paywall + Settings subscription row now taps to paywall); Delete Account already wired in Settings.
- ✅ `[rm]` Deleted `lib/features/community/dm/**` (9 files) + the DM entry point in `community_screen.dart`.
- ✅ `[rm]` Drift: dropped `DmMessages` table + `dm_dao.dart`/`.g.dart`; schemaVersion 12→13 with a `DROP TABLE` migration; regenerated.
- ✅ `[rm]` Supabase: `005_drop_dm.sql` drops `dm_messages`/`dm_conversations` (CASCADE).
- ⏳ `[verify]` Offline gate behavior — needs on-device testing (see test guide).

### Phase 2 — Social graph (followers + friends) + public profiles ✅ BUILT + VERIFIED (2026-05-31, local Supabase)
- ✅ `[new]` `supabase/migrations/006_social.sql`: `follows` + RLS (own-write, authenticated-read), `friends` view (mutual), `public_profiles` view (safe columns + follower/following/friend counts). Proven via psql AND end-to-end through PostgREST.
- ✅ `[new]` Drift `Follows` cache (schema 14→15) + `FollowDao`; client-generated UUID id so unfollow targets the row on delete-sync.
- ✅ `[new]` `lib/features/social/`: `FollowRepository` (optimistic follow/unfollow → sync outbox; relationship/counts from server, offline-safe), `follow_providers.dart`, `public_profile.dart`, `widgets/follow_button.dart`.
- ✅ `[fix]` `profile_screen.dart` `_StatsRow` shows real following (local) + followers (server) counts — hardcoded `9`/`120` removed.
- ✅ `[new]` Public profile fetch via `public_profiles` view (safe fields only — subscription/fcm/trial stay private). Relationship self/none/following/friends computed.
- ✅ `[verify]` `tool/verify_social.py` — 7/7 end-to-end checks pass; `flutter analyze` 0/0; 75 tests pass.
- ⏳ `[defer]` "View another user + tap FollowButton" has no live entry point yet (feed=Phase 3, leaderboard=Phase 5 still mock). Button + data layer built and ready to wire. Manual on-device smoke test pending.
- ⚠️ Pre-existing: repo migrations don't apply cleanly from scratch (`002` refs `exercises`, `003` refs `dm_conversations`, nothing creates them). Fix before a clean cloud `db reset`.

### Phase 3 — Community feed backend ✅ BUILT + VERIFIED (2026-06-01, local Supabase)
- ✅ `[new]` **Migration 007** — `handle_new_user` trigger auto-creates a `user_profiles` row (display_name + 7-day trial) on signup; `has_active_subscription` now honors the profile trial window. Fixes two foundational gaps (no server-side profiles; trial users couldn't read the feed) — also makes Phase 2 real for actual users.
- ✅ `[new]` `SupabaseCommunityRepository` (drop-in for the interface): paginated `fetchFeed` (posts + `post_likes(count)`/`post_comments(count)` embeds + author from `public_profiles` + likedByMe), `createPost` (sanitized), `toggleLike`. Mock kept as offline fallback.
- ✅ `[new]` `community_providers.dart` (repo + `communityFeedProvider`); `community_models.dart` (real `CommunityPost`); `community_mock_data.dart` re-exports the model so `profile_screen` is untouched.
- ✅ `[fix]` `community_screen`: feed driven by the provider (loading/empty/error), **composer now creates a post and refreshes the feed**, like cluster tappable (toggles + tints).
- ✅ `[verify]` `tool/verify_feed.py` — 6/6 (cross-user visibility, author name, like count). `flutter analyze` 0/0; 75 tests pass.
- ⏳ `[defer]` Image upload in composer (text posts work; image button is a no-op for now), comment composer (counts show; top-comments preview only on mock), profile Posts tab still shows mock (its own follow-up). **Cloud needs `007` applied (`supabase db push`) before cloud testing — local already has it.**

### Phase 4 — Challenges (curated + community) + moderation
- 🔴 `[new]` Supabase `challenges`, `challenge_participants`, `challenge_reports` + RLS.
- 🔴 `[new]` Curated daily challenge source (table + cron/edge seeding).
- 🔴 `[new]` Community challenge creation (`pending_review`/`active` per policy).
- 🔴 `[new]` Join + progress tracking; completion → `award-challenge-points` (or trigger) → points + `notify-social-challenge`.
- 🔴 `[new]` Moderation: report action + `moderate-challenges` (hide over-reported) + review queue.
- 🟡 `[fix]` Wire the existing Challenges tab UI (`leaderboard_screen.dart`) to real data.

### Phase 5 — Leaderboard (composite scoring; Global + Friends + Rivals; seasons)
- 🔴 `[new]` Supabase `leaderboard_scores` (board ∈ all_time/weekly/monthly, `season_start`) + indexes.
- 🔴 `[new]` `compute-leaderboard` — composite = avg(streak_norm, volume_norm, points_norm), percentile-normalized; weekly/monthly aggregate **since season_start**.
- 🔴 `[new]` `season_winners` table + `finalize-season` job (weekly Mon / monthly 1st, UTC): snapshot winners, "season ended" push, advance season; feed achievements/earned skins.
- 🔴 `[new]` Friends scope: `leaderboard_friends(board)` RPC.
- 🔴 `[new]` Rivals scope: `rival_pods`/`rival_pod_members` + `assign-rivals` (weekly, similar composite+experience+volume) + `leaderboard_rivals(board)` RPC.
- 🔴 `[new]` `[fix]` Replace mock rows with a Riverpod provider (top-N + own rank); add **board switch** (All-time/Weekly/Monthly) to the existing scope switch; reset countdown + last-winner banner.
- 🔴 `[new]` Drift `LeaderboardCache` (all scopes/boards) for offline display.

### Phase 6 — Anatomy volume, skins economy, charts, reports, calendar, share cards, accent, l10n
- 🔴 `[new]` Anatomy **Volume mode** + Recovery|Volume toggle; smooth transitions; both genders.
- 🔴 `[new]` Skins economy: `skins` catalog + `skin_ownership` + `user_profiles.active_skin_id`; persist selection (replace plain `selectedSkinProvider`).
- 🔴 `[new]` Skins gallery (owned/earnable/buyable) + preview; purchasable via RevenueCat one-time → `purchase-skin` verify; earned via `evaluate-earned-skins`.
- 🔴 `[new]` Progress charts (compare identical past sessions: volume/top set/est-1RM).
- 🔴 `[new]` Periodic reports: `user_reports` + `generate-reports` (weekly+monthly, metrics+deltas); tone push; **Reports window** from Workout→Status→Reports; Drift `UserReports` cache.
- 🔴 `[new]` Training calendar: Home day-strip tap → month calendar of worked days (local `Sessions`) → day detail.
- 🔴 `[new]` Share cards on session completion (image; share + optional post).
- 🔴 `[fix]` Light-mode **orange** accent: make `accent` brightness-resolved in `constants.dart` (light ≠ lime); apply to accent text, selected nav icon, Start Workout button, streak icon.
- 🟡 `[fix]` Notification force-kill resync on resume; verify reminder cron + server-side tone/locale.
- 🟡 `[fix]` Backfill ~84 missing keys in `app_de/es/fr.arb`; no English leakage (except tone samples).

### Phase 7 — Wearables & Health
- 🔴 `[new]` Add `health` package to `pubspec.yaml`; HealthKit entitlement + usage strings (iOS); Health Connect permissions + manifest (Android).
- 🔴 `[new]` Settings "Health/Wearable" connect screen + contextual, revocable permission flow.
- 🔴 `[new]` Read HR/energy/bodyweight/external workouts → merge into sessions/status/recovery.
- 🔴 `[new]` Add `Sessions` fields avgHeartRate/maxHeartRate/activeEnergyKcal (Drift migration + Supabase migration; sync outbox).
- 🔴 `[new]` Write completed sessions back as HealthKit/Health Connect workouts.
- 🔴 `[new]` Apple Watch live heart rate during active session (HealthKit workout session).
- 🔴 `[new]` Verify full functionality with Health denied/unavailable.

### Phase 8 — Release prep
- 🔴 `[new]` Apple Developer account (blocks APNs, Live Activities, HealthKit, on-device IAP, submission).
- 🔴 `[new]` App Store Connect + Play Console entries; listings, screenshots, privacy manifest, ATT, age rating, Health-data disclosures.
- 🔴 `[new]` Production subscription products + RevenueCat entitlements + skin IAP products.
- 🟡 `[fix]` Crashlytics verified; smoke test on physical iOS + Android.
- 🔴 `[new]` Tests: subscription state machine, sync resolution, leaderboard scoring (all scopes), rivals matching, recovery/volume math, tone resolution, report deltas.
- 🟡 `[fix]` CI green: analyze + test + build; signed builds both stores.

---

## 3. Notable Corrections vs. Earlier Audit
- **Paywall gate is partial, not absent** — gate logic exists in `my_gym_bro_scaffold.dart` but uses a dismissible `push`; needs hardening.
- **14 skins are wired, not 3** — selection works; the gap is the *economy* (ownership/earn/buy/persistence), not the picker.
- **Leaderboard scope UI already exists** (Rivals/Global/Friends) — missing pieces are season boards, real data, and scoring.
- **Profile follower/following columns exist** but are hardcoded.

---

**End of STATUS**
